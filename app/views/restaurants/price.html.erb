		<div class="wrapper-grey padded">
			<div class="container">
				<h1 class="text-center">Restaurants by price <br><br><br></h1>
				<% sorted = @restaurants.sort_by { |k| k["price"] } %>
				<% sorted.each do |restaurant| %>

				<div class="row">
					<div class="col-xs-12 col-sm-4">
						<div class="card" style="background: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('<%= cl_image_path restaurant.photo, width: 500, height: 180, crop: :fill %>');">
							<div class="card-category"><%= restaurant.category%></div>
							<div class="card-description">
								<h2><%=restaurant.name%></h2>
								<p><%= restaurant.description%></p>
								<h6>Average price per person <%=restaurant.price%>€ </h6>
							</div>

						</div>
					</div>
					<%end%>
				</div>
				<%= link_to "Add a new restaurant", new_restaurant_path %>
			</div>
		</div>
	</div>
